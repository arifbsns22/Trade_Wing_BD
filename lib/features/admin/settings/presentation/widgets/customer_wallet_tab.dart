import 'package:flutter/material.dart';

class CustomerWalletTab extends StatelessWidget {
  const CustomerWalletTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Customer Wallet Settings (Coming Soon)'),
    );
  }
}
